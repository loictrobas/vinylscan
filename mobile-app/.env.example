# Backend URL baked in at build time. For local dev with a physical phone,
# use your Mac's LAN IP (not localhost) — see CLAUDE.md. Can also be
# overridden at runtime in-app (tap the WiFi icon in the scan screen header).
VITE_API_URL=http://localhost:8000
